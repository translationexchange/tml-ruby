# encoding: UTF-8

require 'spec_helper'

describe Tml::Decorators::Default do
  describe "#initialize" do
    it "sets language attributes" do
      # decorator = Tml::Decorators::Default.new(:language => "a")
      # pp decorator
    end
  end 
end